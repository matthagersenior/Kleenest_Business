import { useCallback,useEffect,useState } from 'react';
import { ActivityIndicator,Pressable,RefreshControl,ScrollView,Text,TextInput,View } from 'react-native';
import { createBusinessLocation,deleteBusinessMedia,listBusinessLocations,listBusinessMedia,listLocationAmenities,setBusinessLocationActive,setLocationAmenity,updateBusinessLocation,type BusinessAmenity,type BusinessLocation,type BusinessMedia } from '@/services/business';import { useBusinessWorkspace } from '@/state/businessWorkspace';
const empty={name:'',address:'',city:'',state:'',postalCode:'',phone:'',website:''};export default function Screen(){const{workspace,access,refresh:rw}=useBusinessWorkspace(),[locations,setLocations]=useState<BusinessLocation[]>([]),[media,setMedia]=useState<BusinessMedia[]>([]),[amenities,setAmenities]=useState<Record<string,BusinessAmenity[]>>({}),[form,setForm]=useState(empty),[editing,setEditing]=useState<BusinessLocation|null>(null),[amenityId,setAmenityId]=useState(''),[refreshing,setRefreshing]=useState(false),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null);const load=useCallback(async()=>{if(!workspace)return;const[l,m]=await Promise.all([listBusinessLocations(workspace.business_id),listBusinessMedia(workspace.business_id)]);const a=await Promise.all(l.map(async x=>[x.id,await listLocationAmenities(workspace.business_id,x.id)] as const));setLocations(l);setMedia(m);setAmenities(Object.fromEntries(a));},[workspace]);useEffect(()=>{load().catch(c=>setError(String(c))).finally(()=>setLoading(false));},[load]);async function reload(){setRefreshing(true);try{await Promise.all([load(),rw()]);}finally{setRefreshing(false)}}async function save(){if(!workspace||!form.name.trim())return;try{if(editing)await updateBusinessLocation(editing.id,{name:form.name,address:form.address||null,phone:form.phone||null,website:form.website||null,active:Boolean(editing.is_active??editing.active??true)});else{if(access?.location_limit!=null&&locations.length>=access.location_limit)throw new Error('Location entitlement reached. Enterprise is required beyond this limit.');await createBusinessLocation(workspace.business_id,{...form,name:form.name.trim()});}setEditing(null);setForm(empty);await reload();}catch(c){setError(c instanceof Error?c.message:String(c));}}if(loading)return <ActivityIndicator/>;return <ScrollView refreshControl={<RefreshControl refreshing={refreshing} onRefresh={reload}/>} contentContainerStyle={{padding:16,gap:14,paddingBottom:50}}>{error?<Text style={{color:'#922'}}>{error}</Text>:null}<View style={card}><Text style={title}>{locations.length} locations · management cluster</Text><Text>{access?.location_limit==null?'Enterprise-scale entitlement':`${access.location_limit} location entitlement`}</Text></View><View style={card}><Text style={title}>{editing?'Edit location':'Add location'}</Text>{(['name','address','city','state','postalCode','phone','website'] as const).map(k=><TextInput key={k} value={form[k]} onChangeText={v=>setForm(s=>({...s,[k]:v}))} placeholder={k} style={input}/>)}<Button label={editing?'Save location':'Create location'} onPress={save}/></View>{locations.map(l=>{const active=Boolean(l.is_active??l.active??true),a=amenities[l.id]??[],m=media.filter(x=>x.location_id===l.id);return <View key={l.id} style={card}><Text style={title}>{l.name}</Text><Text>{[l.address,l.city,l.state].filter(Boolean).join(', ')||'Address incomplete'} · {active?'ACTIVE':'INACTIVE'}</Text><Text>{a.length} amenities · {m.length} media</Text><View style={row}><Button label="Edit" onPress={()=>{setEditing(l);setForm({name:l.name,address:String(l.address??''),city:String(l.city??''),state:String(l.state??''),postalCode:String(l.postal_code??''),phone:String(l.phone??''),website:String(l.website??'')})}}/><Button label={active?'Deactivate':'Activate'} onPress={async()=>{await setBusinessLocationActive(l.id,!active);await reload()}}/></View><Text style={{fontWeight:'800'}}>Amenities</Text>{a.map(x=><View key={x.amenity_id} style={row}><Text style={{flex:1}}>{x.name}</Text><Button label="Remove" onPress={async()=>{if(workspace){await setLocationAmenity(workspace.business_id,l.id,x.amenity_id,'remove');await reload()}}}/></View>)}<View style={row}><TextInput value={amenityId} onChangeText={setAmenityId} placeholder="Amenity UUID" style={[input,{flex:1}]}/><Button label="Add" onPress={async()=>{if(workspace&&amenityId.trim()){await setLocationAmenity(workspace.business_id,l.id,amenityId.trim(),'add');setAmenityId('');await reload()}}}/></View><Text style={{fontWeight:'800'}}>Media</Text>{m.map(x=><View key={x.id} style={row}><Text style={{flex:1}}>{x.caption??x.storage_path}</Text><Button label="Delete" onPress={async()=>{if(workspace){await deleteBusinessMedia(workspace.business_id,x.id);await reload()}}}/></View>)}</View>})}</ScrollView>}
const card={backgroundColor:'white' as const,padding:15,borderRadius:16,gap:9},title={fontSize:18,fontWeight:'800' as const},input={borderWidth:1,borderColor:'#dce4df',borderRadius:12,padding:11},row={flexDirection:'row' as const,flexWrap:'wrap' as const,gap:8,alignItems:'center' as const};function Button({label,onPress}:{label:string;onPress:()=>void}){return <Pressable onPress={onPress} style={{backgroundColor:'#173f2d',borderRadius:999,paddingHorizontal:12,paddingVertical:8}}><Text style={{color:'white',fontWeight:'800'}}>{label}</Text></Pressable>}
